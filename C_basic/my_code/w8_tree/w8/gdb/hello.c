#include <stdio.h>
// todo: thử dịch với -g
//     gdb test
//     (gdb) run
//          tạo điểm dừng: break (dòng 11), continue, step, next
int f() {
  return 0;
}

int main() {
   int j = 0;
   int arr[] = {1, 2, 3};
   // f();
   printf("Hello world: %f", 1.0/j);

   return 0;
}